# Public environment variables for Expo (example)
# Copy to .env and adjust values per environment

# Use mock API client (true for development)
EXPO_PUBLIC_USE_MOCK=true

# Skip auth for development (show TabNavigator directly)
EXPO_PUBLIC_SKIP_AUTH=false

